# Audit of the supplied `suryaML` reference (checked 23 September 2026)

Reference: `https://github.com/macm4d-gif/7006SCN_Coursework/tree/main/suryaML`. The separate `saiashishd/collegework` repo is **not** evidence of the new allocated dataset. This audit describes what the submitted files *show*, not anyone's intentions.

| Issue in supplied reference | What was actually visible | Impact / correction |
| --- | --- | --- |
| Repository rule | `macm4d-gif/7006SCN_Coursework` is **public**, in a personal account rather than `7006SCN2627SEPNOV`. | Subject to the stated 20% cap and −10 visibility deduction if used for submission. Create a **private** repo inside the required module organisation; ask the Module Leader if inaccessible. |
| Dataset allocation | README/report claim `TR-04`, but the Aula student allocation register was **not provided**. | Must be checked against your own row. Do not set `confirmed_against_aula_register=true` without doing this. |
| Real model execution | `Task1.ipynb` has 3 cells; the count cell prints literal numbers. `Task2.ipynb` prints four fixed scores. `Task3.ipynb` prints fixed optimisation/stability claims; `Task4.ipynb` prints a fixed export message. | No reproducible ingestion, stage fit, CrossValidator, confusion, or explanation was shown by those notebooks. The replacement computes outputs from real Spark jobs. |
| Result-file check | `task2.json` has only **two** algorithm objects and just ROC-AUC values, despite README claiming four tuned models. `task1.json` lacks source URL, licence, pipeline and Spark configuration. | Replacement writes the required structured facts after actual runs. Do not submit hard-coded data. |
| Tableau | Original `7006SCN_NYC_Taxi_Dashboards.twb` XML has **zero datasources, worksheets and dashboards**. The supplied Tableau Public URL redirects to a **404** page at time of audit. | A real Tableau workbook must be built, published and screened manually. Four genuine dashboard screenshots form the contact sheet. |
| Version history | The linked public repo showed **two** commits as of audit, both dated 23 September 2026. README lists twelve other hashes/weekly results not present in its history. | Remove unsupported hash claims. Make real, chronological commits as you develop; never forge/backdate a history. |
| Methodological claims | Source report describes SHAP `TreeExplainer` of Spark GBT and precise fairness/scale costs, without executable analysis or model evidence. It treats borough disparity as a legal disparate-impact finding. It claims a 71:29 low:high tipping ratio; the real January **credit-card-only** subset read by Spark had 1,483,615 low-tip and 4,001,547 high-tip records (about 27:73). | Replacement computes class weights from the actual training rows, offers Spark-backed LIME on a small sample, and performs a descriptive geographic subgroup audit. These are not legal or causal conclusions. |
| Actual assigned-source scan | After the user confirmed TR-04, a **read-only Spark scan** of all twelve downloaded official 2019 Parquet files on 23 September 2026 observed **84,598,444 raw rows, 19 original columns in every month, and 1,243,532,931 downloaded bytes (~1.158 GiB)**. The old README/JSON claim 84,368,012 rows, 18 columns and 2.57 GiB compressed, without a matching notebook computation. | The source passes volume/column/file-size thresholds **in this run**. Task1 still needs to be run under the student's identity, licence decision and cluster, and must record the figures from THAT run. See `scripts/verify_full_source.py`. |

**Do not submit the old text, screenshots, CSVs, `results/*.json` or workbook as if supported by a run.** Run the replacement code on your confirmed allocation and interpret your own actual outputs. Neither this audit nor the software guarantees a mark.
