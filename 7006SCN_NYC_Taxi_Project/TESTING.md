# Verification log (24 September 2026; tests and smoke are synthetic)

Environment used to check software correctness: PySpark **3.5.9**, Python **3.11.16**, Java **11**, single local Spark worker with `spark.driver.memory=512m` for smoke. This is **not** an assessed run on the allocated 2019 dataset or a valid coursework executor benchmark.

- `PYTHONPATH="$PWD" PYSPARK_PYTHON="$PWD/.venv/bin/python" PYSPARK_DRIVER_PYTHON="$PWD/.venv/bin/python" SPARK_LOCAL_IP=127.0.0.1 .venv/bin/python -m pytest tests -q`: **17 passed, 1 PySpark deprecation warning** (latest after inspecting the uploaded official answer sheet). Tests cover schema drift, source metadata, card-label/leakage assertions; real Spark synthetic fits/transforms/metrics across **linear LR, bagged RF, neural MLP and free-choice GBT** (fixed width 53); four saved models scoring the SAME frozen perturbed Nov–Dec-style test cohort with ΔF1/ΔROC-AUC and ranks; deterministic feature-only test perturbation; borough fairness; Task4 exports/contact-sheet gate; strict report-draft synthetic fixture; and **38 unchanged university-answer-sheet tables** in an explicitly incomplete Word working copy. The source-only ZIP test confirms both answer sheets are included but not student config/assessed results. Each notebook still has ten sequential headings, all code cells compile and all outputs are empty until a real assessed run. Example configs leave executor count to the cluster and reject invalid explicit counts.
- `.venv/bin/python NYC_Taxi_TR04.py --plan`, `.venv/bin/python scripts/run_project.py --plan`, their `--help` options, and `.venv/bin/python -m compileall -q NYC_Taxi_TR04.py coursework scripts tests`: passed for both CLI entry points and modular sources. The plan mode starts NO Spark job and prints NO measured scores.
- `python scripts/verify_full_source.py ...`: after the user confirmed TR-04, downloaded all 12 **official** monthly 2019 files and performed a read-only Spark scan. Observed **84,598,444 raw rows; 19 original columns in each month; 1,243,532,931 actual file bytes (1.1581 GiB)**. After the defined credit-card/valid-label filters, **60,614,249** rows remain (15,075,444 low-tip; 45,538,805 high-tip; positive share **75.13%**); time split Jan–Sep 45,546,638, October 5,215,333, Nov–Dec 9,852,278. This confirmed ingestion and disproved the original repository's 71:29 low:high ratio, but created **no assessed results JSON, model metrics or report**.
- **Full data-ingestion/write dry run:** On the same official 12-month files, Spark wrote **60,614,249** eligible records to month-partitioned Parquet using a 32-way `(pickup_date, PULocationID)` shuffle, then re-read and counted all 12 months. This took ~308 seconds on a constrained local single worker **after moving shuffle scratch off a 1-GB tmpfs**; compressed processed output was ~328 MB. These timings/partitions are a functional dry run, NOT your university-cluster optimisation evidence.
- `python scripts/smoke_all.py`: rerun **AFTER replacing ambiguous LinearSVC with a true neural-family Spark MLP**. All FOUR 2-fold SYNTHETIC CrossValidators + full synthetic refits, October threshold selection and test scoring passed. Tiny smoke grids are intentionally unassessable; the script cannot write `results/task*.json`. Do not put synthetic test metrics/timings in the report.
- **Older real-data 0.2% MODEL SMOKE (NOT assessed; PRE-neural update):** Prior to the attached-sheet revision, Spark sampled 121,058 of 60.6M processed rows and ran the then-current four small CV/refit/test paths (including LinearSVC), plus a small RF LIME call. That check **does not validate the CURRENT neural model on actual taxi data**; current MLP and the new test-perturbation analysis were exercised with synthetic Spark data only. Never cite this old sample's numbers/timings as assessable.
- LIME check: fitted/saved a **synthetic** Spark RandomForest pipeline, ran `explain_with_lime` through the actual Spark `PipelineModel` on 100 local perturbations; six local feature weights were produced. The synthetic local surrogate fit was not used as a claim about the real model.
- `scripts/check_submission.py --report report/TR04_Official_Answer_Sheet_WORKING_INCOMPLETE.docx` intentionally exits **1** with **22 blocking / 6 manual checks**: no student identity, signed MS Forms screenshot, truthful AI declaration and prompt log, measured Task 1–4 JSON, completed notebook outputs, real Git history/repository, active links, word count or genuine assessed figures. This fail-closed status is expected and is NOT a bad test result.

**Not tested here:** the NEW neural classifier and test perturbation on the actual 60.6M-row taxi cohort; full-scale four-grid 3-fold CV on Jan–Sep plus four ~45.5M-row refits; actual student-identified Task1 JSON/evidence; university-cluster Spark UI profiling; Tableau Desktop/Public publication; marker access; independent Aula allocation-register verification; dataset reuse terms; teaching-week commits and report grading. Full PySpark work needs appropriately provisioned compute and the confirmed allocation row; do not interpret synthetic smoke outputs as measurements of NYC TLC data.
